#pragma once
void CheckInBook(char* Book);
void CheckOutBook(char* Book);
void ShowBook();
int Plus_BookCount(char* OneLine, char* Book);
int Minus_BookCount(char* OneLine, char* Book);
int BookCounter(char* OneLine, char* Book, int LineNum);
void MaxBook();

